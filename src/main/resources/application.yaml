spring:
  datasource:
    url: jdbc:postgresql://postgresql:5432/star-wars-db
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
  flyway:
    baseline-on-migrate: true
